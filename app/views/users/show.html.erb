<h1>ユーザー詳細</h1>
<% breadcrumb :user_show, @user %>
  <%= @user.last_name %>
  <%= @user.first_name %>
  <%= @user.email %><br>
  <% @user.teams.each do |team| %>
    <%= team.name %><br>
  <% end %>

<% if @user.id == current_user.id%>
  <%= link_to "ユーザーを削除する", user_path(@user.id), method: :delete, data: {confirm: "参加しているチームからも削除されますが、本当に削除しますか？"} %>
<% end %>