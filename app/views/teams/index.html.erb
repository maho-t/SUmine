<h2>グループ一覧</h2>
<% breadcrumb :root %>
<div class="team-create">
  <%= link_to "新規グループの追加", new_team_path, class: "btn btn-outline-danger" %>
</div>
<div class="team-list">
  <p class="note-text">※ 既存のグループに参加するには、グループメンバーから追加をしてもらってください。</p>
  <ul class="list-group">
    <% @teams.each do |team| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <%= link_to team.name, team_path(team.id), class: "link" %>
        <span class="badge bg-secondary rounded-pill"><%= team.users.count%></span>
      </li>
    <% end %>
  </ul>
</div>
