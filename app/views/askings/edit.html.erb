<h2>問い合わせ</h2>
<% breadcrumb :asking_edit, @team, @asking %>
<div class="asking-container">
  <div class="asking">
    <div class="asking-body">
      <div class="asking-all">問い合わせ編集</div>
    </div>
  </div>

  <div class="asking-form">
    <%= render partial: "form", locals: { team: @team, asking: @asking, edit_flag: true } %>
  </div>
</div>
